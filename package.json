{
  "name": "mock-proxy",
  "version": "1.0.0",
  "engines": {
    "node": ">=8.14.0"
  },
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node build/app.js",
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w  -p tsconfig.json",
    "jest": "jest .",
    "eslint": "eslint --config .eslintrc.json ./src/**/*.js",
    "eslint-fix": "eslint --config .eslintrc.json ./src/**/*.js --fix",
    "reverse": "adb reverse tcp:3000 tcp:3000",
    "backup": "cp -R data/ ../data/"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.1.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "@types/node": "^12.7.8",
    "eslint": "^6.4.0",
    "eslint-plugin-jest": "^22.17.0",
    "jest": "^24.9.0",
    "ts-jest": "^24.1.0",
    "typescript": "^3.6.3"
  }
}
